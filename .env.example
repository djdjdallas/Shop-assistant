# =============================================================================
# Product Admin Sidekick - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# See SHOPIFY_SETUP_GUIDE.md for detailed instructions.
# =============================================================================

# ---------------------
# Shopify App Credentials
# ---------------------
# Found in your Shopify Partner Dashboard → Apps → [Your App] → API credentials
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret

# OAuth scopes the app requests during installation
SHOPIFY_SCOPES=read_products,write_products,read_orders,read_inventory

# Your production app URL (Vercel deployment URL)
# Example: https://product-admin-sidekick.vercel.app
SHOPIFY_APP_URL=https://your-app.vercel.app

# Public Shopify API key (exposed to client for App Bridge)
# This is the same value as SHOPIFY_API_KEY
NEXT_PUBLIC_SHOPIFY_API_KEY=your_shopify_api_key

# ---------------------
# Supabase Configuration
# ---------------------
# Found in your Supabase Dashboard → Settings → API
# Project URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon/public key (safe to expose to client, used with Row Level Security)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key (SECRET - server-side only, bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Direct database connection string (used for running migrations only)
# Found in Supabase Dashboard → Settings → Database → Connection string (URI)
# DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# ---------------------
# Development Settings
# ---------------------
# Fallback shop ID for local development without Shopify OAuth
# Only used when NODE_ENV=development and no auth token is present
DEFAULT_SHOP_ID=dev-shop-id

# Node environment (development | production)
NODE_ENV=development
